import { assert } from 'chai';
import * as constants from '../../../src/core/constants';

describe('constants Unit', function () {
  it('should exist', function () {
    assert.isOk(constants);
  });
});
