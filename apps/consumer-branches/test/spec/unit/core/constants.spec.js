import { assert } from 'chai';
import * as constants from '../../../../src/core/deprecated/constants';

describe('Constants Module', function () {
  it('should exist', function () {
    assert.isOk(constants);
  });
});
